import { Transaction } from '../../../gen/audiotool/document/v1/document_service_pb';

/** Use this flag to check if wasm was already initialized. */
export declare let wasmReady: boolean;
export declare const loadLocalWasms: () => Promise<void>;
export declare const loadWasms: (config: {
    documentValidatorWasmUrl: string;
    documentValidatorWrapperUrl: string;
}) => Promise<void>;
export type WasmDocumentState = {
    applyTransaction: (transaction: Transaction) => Transaction | string;
};
/** Create a wasm document state. `loadWasmFromUrls` must have been called and awaited,
 * otherwise this method will throw.
 */
export declare const createWasmDocumentState: () => WasmDocumentState;
