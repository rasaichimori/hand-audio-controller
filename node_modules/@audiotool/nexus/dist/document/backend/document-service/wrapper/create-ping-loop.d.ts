import { DocumentService } from '../../../../gen/audiotool/document/v1/document_service_connect';
import { RetryingClient } from '../../../../utils/grpc/retrying-client';
import { ValueNotifier, ObservableValue } from '../../../../utils/observable-notifier-value';

/** Creates a value notifier that updates the current ping at most every
 * intervalMs times. Doesn't update as long as no ping returns (todo)
 */
export declare const createPingNotifier: (documentService: RetryingClient<typeof DocumentService>, projectName: string, intervalMs: number, connectionOk: ValueNotifier<boolean>) => ObservableValue<number>;
