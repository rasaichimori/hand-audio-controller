import { EntityMessage, EntityTypeKey } from '../entity-utils';
import { NexusEntity } from '../entity';

/** Converts a nexus entity to a proto entity message, that can easily be wrapped
 * in the container `Entity` message.
 */
export declare const entityToPbMessage: <T extends EntityTypeKey>(entity: NexusEntity, assignNewId?: boolean) => EntityMessage<T>;
