import { Modification } from '../../gen/audiotool/document/v1/document_service_pb';
import { EntityQuery } from '../query/entity';

/** @internal Builds a modification that removes an entity and all entities that depend on it. */
export declare const buildModificationForRemoveWithDependencies: (id: string, query: EntityQuery) => Modification[];
