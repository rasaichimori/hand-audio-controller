import { NexusEntity } from '../entity';
import { EntityConstructorType, EntityTypeKey } from '../entity-utils';
import { Modification } from '../../gen/audiotool/document/v1/document_service_pb';
import { DeepPartial } from 'utility-types';

/**
 * @internal
 *  Builds a new `Create` modification that clones an existing entity,
 * optionally overwriting fields.
 *
 * Returns the modification, and for simplicity the entity id.
 */
export declare const buildModificationForEntityClone: <T extends EntityTypeKey>(entity: NexusEntity<T>, args?: DeepPartial<EntityConstructorType<T>>) => {
    modification: Modification;
    entityId: string;
};
