import { Modification } from '../../gen/audiotool/document/v1/document_service_pb';

/** @internal Builds a modification that removes an entity. */
export declare const buildModificationForRemove: (id: string) => Modification;
