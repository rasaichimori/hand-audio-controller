import { EntityConstructorType } from '../entity-utils';
import { NexusLocation } from '../location';

/** Takes a constructor type argument, clones it, and applies a mapping
 * function to all locations.
 */
export declare const mapConstructorLocations: <C extends EntityConstructorType>(cons: C, fn: (loc: NexusLocation) => NexusLocation) => C;
