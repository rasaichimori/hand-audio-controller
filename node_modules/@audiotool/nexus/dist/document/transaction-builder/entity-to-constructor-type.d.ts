import { EntityConstructorType, EntityTypeKey } from '../entity-utils';
import { NexusEntity } from '../entity';

/** Takes an entity, and returns that entity as `ConstructorType` */
export declare const entityToConstructorType: <T extends EntityTypeKey>(entity: NexusEntity<T>) => EntityConstructorType<T>;
