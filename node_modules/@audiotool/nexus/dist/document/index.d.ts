/**
 * @packageDocumentation
 *
 * This module contains mostly types related to the nexus document.
 */
/**
 * Union type mapping all entity type keys to their corresponding entity types.
 *
 * This is a comprehensive type that includes all possible entity types in the document,
 * such as audio devices, mixer channels, timeline tracks, etc.
 */
export type { EntityTypes } from '../gen/audiotool/document/v1/utils/types';
/**
 * Type representing an entity with overwritten fields.
 *
 * Used in transaction building when cloning or modifying entities with specific field overrides.
 */
export type { EntityWithOverwrites } from './transaction-builder/build-clone-linked-entities';
/**
 * Enum representing the different types of targets that can be referenced in the document.
 *
 * Used to mark entities like grooves, presets, and other referenceable objects.
 */
export { TargetType } from '../gen/audiotool/document/v1/opt/opt_pb';
/**
 * The main interface for interacting with synced Audiotool documents.
 *
 * @see {@link SyncedDocument} for detailed documentation
 */
export type { SyncedDocument } from '../audiotool-client';
/**
 * Base type representing any entity in the Nexus document.
 *
 * Provides type-safe access to entity fields and methods for querying and manipulation.
 */
export type { NexusEntity } from './entity';
/**
 * Type representing a key that identifies an entity type.
 *
 * Examples: "bassline", "heisenberg", "mixerChannel", "noteTrack", etc.
 * Used as a type parameter to ensure type safety when working with specific entity types.
 */
export type { EntityTypeKey } from './entity-utils';
/**
 * Interface for managing and subscribing to document events.
 *
 * Provides methods to listen for changes, entity updates, and other document events.
 */
export type { NexusEventManager } from './event-manager';
export type { ArrayField, NexusField, PrimitiveField, PrimitiveType, } from './fields';
/**
 * Type representing a location within the document schema.
 *
 * Used to reference specific fields or nested structures within entities.
 */
export type { NexusLocation } from './location';
/**
 * Type representing a generic object structure in the Nexus document.
 *
 * Used for type-safe access to nested object fields within entities.
 */
export type { NexusObject } from './object';
/**
 * Type for querying entities in the document.
 *
 * Provides a fluent API for finding entities by type, ID, or other criteria.
 */
export type { EntityQuery, ReferenceQuery } from './query/entity';
/**
 * Type for querying fields within entities.
 *
 * Provides a fluent API for accessing and filtering entity fields.
 */
export type { FieldQuery } from './query/field';
export { schemaLocationToSchemaPath, schemaPathToSchemaLocation, } from './schema/converters';
export * from './schema/schema-details';
/**
 * Type representing a schema location as a string identifier.
 *
 * Used to reference fields in the schema using dot notation (e.g., "stompboxDelay.delayTime").
 */
export type { SchemaLocation } from './schema/schema-location';
/**
 * Type containing detailed information about a schema location.
 *
 * Includes type information, validation rules, default values, and other metadata
 * for a specific field in the schema.
 */
export type { SchemaLocationDetails } from './schema/schema-location-details';
export type { SafeTransactionBuilder, TransactionBuilder, } from './transaction-builder';
