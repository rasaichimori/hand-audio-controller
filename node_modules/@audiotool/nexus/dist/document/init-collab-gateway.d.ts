import { NexusGateway } from './backend/gateway';
import { DocumentService } from '../gen/audiotool/document/v1/document_service_connect';
import { RetryingClient } from '../utils/grpc/retrying-client';

/** Initialize the collab gateway. Wasm must be loaded at this point. */
export declare const initCollabGateway: (projectName: string, documentService: RetryingClient<typeof DocumentService>) => NexusGateway;
