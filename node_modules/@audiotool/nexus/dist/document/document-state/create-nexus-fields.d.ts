import { AnyMessage, Message, ScalarType } from '@bufbuild/protobuf';
import { EntityTypeKey } from '../entity-utils';
import { NexusLocation } from '../location';
import { Update } from '../../gen/audiotool/document/v1/document_service_pb';
import { NexusFieldTypes } from '../object';

/**
 * Creates the `fields` object for a `NexusObject` based on a protobuf message.
 * `location` is the location of the `NexusObject`.
 */
export declare const createNexusFields: (getEntityType: (id: string) => EntityTypeKey, entityType: EntityTypeKey, message: Message<AnyMessage>, location: NexusLocation) => NexusFieldTypes;
/**
 * Object used to convert protobuf ScalarType to the field names used in
 * the protobuf update message of that type.
 */
export declare const scalarTypes: Record<ScalarType, NonNullable<Update["value"]["case"]>>;
export declare const mustExtractMutability: (type: EntityTypeKey, fieldIndex: readonly number[]) => boolean;
