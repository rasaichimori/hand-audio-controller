import { EntityMessage, EntityTypeKey } from '../entity-utils';
import { NexusEntity } from '../entity';

/** Create a nexus entity from a proto message */
export declare const createEntity: <T extends EntityTypeKey>(getEntityType: (id: string) => EntityTypeKey, entityMessage: EntityMessage) => NexusEntity;
