import { EntityDetails, SchemaDetails } from './schema-details';
import { _SchemaLocator } from './schema-locator';

/** Based on a schema locator, returns the schema path details.
 *
 * This is an internal method
 */
export declare const _getSchemaLocatorDetails: (path: _SchemaLocator) => SchemaDetails;
/** Return the schema info on each segment. */
export declare const getAllSchemaDetailsFromLocator: (location: _SchemaLocator) => [EntityDetails, ...Exclude<SchemaDetails, EntityDetails>[]];
