import { ObservableValue } from './observable-notifier-value';
import { Terminable } from './terminable';

export declare class SetNotifier<T> implements ObservableValue<T[]> {
    #private;
    constructor(values: T[]);
    getValue(): T[];
    replace(values: T[]): void;
    add(value: T): void;
    delete(value: T): void;
    has(value: T): boolean;
    clear(): void;
    subscribe(callback: (v: T[]) => void, initialTrigger?: boolean): Terminable;
    terminate(): void;
}
