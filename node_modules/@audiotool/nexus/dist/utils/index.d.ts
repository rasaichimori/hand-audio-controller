/**
 * @packageDocumentation
 * Some utility functions, classes, types, used across the package.
 */
/**
 * A hash map implementation that allows objects to be used as keys.
 *
 * Objects must implement the {@link Hashable} interface to be used as keys.
 * Provides better performance than using objects directly as Map keys.
 */
export { HashMap } from './hash-map';
/**
 * Interface for objects that can be used as keys in a {@link HashMap}.
 *
 * Objects implementing this interface must provide a hash symbol property
 * that returns a unique string identifier.
 */
export type { Hashable } from './hash-map';
/**
 * An asynchronous lock for coordinating access to shared resources.
 *
 * Provides a way to serialize access to resources in async code by allowing
 * code to wait for a lock to be released before proceeding.
 */
export { AsyncLock } from './async-lock';
/**
 * A lock handle returned by {@link AsyncLock.acquire}.
 *
 * Call `release()` to release the lock and allow waiting code to proceed.
 */
export type { Lock } from './async-lock';
/**
 * A notifier that tracks a set of values and notifies observers of changes.
 *
 * Useful for tracking collections and notifying when items are added or removed.
 */
export { SetNotifier } from './observable-notifier-set';
/**
 * A notifier that tracks a map of key-value pairs and notifies observers of changes.
 *
 * Useful for tracking keyed collections and notifying when values change.
 */
export { MapValueNotifier } from './observable-notifier-value';
/**
 * Base notifier class for implementing observable patterns.
 *
 * Provides a foundation for creating observable objects that can notify
 * subscribers of changes.
 */
export { Notifier } from './observable-notifier';
/**
 * Interface for objects that can be observed for changes.
 *
 * Implemented by notifier classes to provide subscription-based change notifications.
 */
export type { Observable } from './observable-notifier';
/**
 * A notifier that tracks a single value and notifies observers when it changes.
 *
 * Useful for tracking state changes and notifying subscribers when the value updates.
 */
export { ValueNotifier } from './observable-notifier-value';
/**
 * Interface for observable values that can be read and subscribed to.
 *
 * Provides a way to observe changes to a single value over time.
 */
export type { ObservableValue } from './observable-notifier-value';
/**
 * Interface for objects that can be terminated or cleaned up.
 *
 * Used to mark objects that have cleanup logic that should be called
 * when they are no longer needed.
 */
export type { Terminable } from './terminable';
export { neverThrowingFetch, type NeverThrowingFetch, } from './fetch/never-throwing-fetch';
/**
 * Transport interface for gRPC connections with keepalive support.
 *
 * Used internally for maintaining persistent connections to gRPC services.
 */
export type { KeepaliveTransport } from './grpc/keepalive-transport';
/**
 * Type representing a gRPC client that automatically retries failed requests.
 *
 * Wraps gRPC service clients to provide automatic retry logic for transient failures.
 */
export type { RetryingClient, RetryOptions } from './grpc/retrying-client';
/**
 * Utility function that throws an error.
 *
 * Useful for type narrowing in TypeScript when you need to assert that a value
 * is not null/undefined or meets certain conditions.
 *
 * @example
 * ```typescript
 * const value = maybeValue ?? throw_(new Error("Value is required"));
 * ```
 */
export { throw_ } from './lang';
/**
 * Creates a typed array with proper TypeScript type inference.
 *
 * Helper function for creating arrays where TypeScript can properly infer
 * the element type from the initial values.
 *
 * @example
 * ```typescript
 * const numbers = createArrayTyped([1, 2, 3]); // Type: number[]
 * ```
 */
export { createTypedArray as createArrayTyped } from './typed-array';
/**
 * Type representing a path to a field in the schema.
 *
 * Used to represent paths to fields in the schema.
 */
export type { SchemaPath } from '../gen/audiotool/document/v1/utils/path';
export { getLoginStatus } from './login-flow';
export type { LoginStatus } from './login-flow';
