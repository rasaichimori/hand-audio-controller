/**
 * @packageDocumentation
 *
 * # Audiotool Nexus SDK
 *
 * The main entry point for the Audiotool Nexus SDK. This package enables real-time
 * collaboration and document manipulation for Audiotool projects.
 *
 * ## Quick Start
 *
 * ```typescript
 * import { createAudiotoolClient } from "@audiotool/nexus";
 *
 * // Create client and set authentication
 * const client = await createAudiotoolClient({pat: "at_pat_your_token_here"});
 *
 * // Connect to a project
 * const document = await client.createSyncedDocument({
 *   mode: "online",
 *   project: "https://beta.audiotool.com/studio?project=abc123"
 * });
 *
 * await document.start();
 *
 * // Access API services
 * const projects = await client.api.projectService.listProjects({});
 * ```
 *
 * ## Main Functions
 *
 * - {@link createAudiotoolClient} - Create the main client instance
 * - {@link getSchemaLocationDetails} - Inspect field types and validation rules
 *
 * ## Key Types
 *
 * - {@link AudiotoolClient} - Main client with document creation and API access
 * - {@link SyncedDocument} - Document interface for transactions and queries
 * - {@link AudiotoolAPI} - Collection of API clients for Audiotool services
 */
/**
 * Get detailed information about a field's type and validation constraints.
 *
 * @example
 * ```typescript
 * import { getSchemaLocationDetails } from "@audiotool/nexus";
 *
 * const details = getSchemaLocationDetails("stompboxDelay", "delayTime");
 * if (details.type === "primitive") {
 *   console.log("Delay time range:", details.primitive.range);
 *   console.log("Default value:", details.primitive.default);
 * }
 * ```
 */
export { getSchemaLocationDetails } from './document/schema/get-schema-location-details';
/**
 * Main function to create an Audiotool client instance.
 *
 * @see {@link createAudiotoolClient} for detailed documentation
 */
export { createAudiotoolClient } from './audiotool-client';
/**
 * The main client interface providing document creation and API access.
 *
 * @see {@link AudiotoolClient} for detailed documentation
 */
export type { AudiotoolClient } from './audiotool-client';
/**
 * The main interface for interacting with synced Audiotool documents.
 *
 * @see {@link SyncedDocument} for detailed documentation
 */
export type { SyncedDocument } from './audiotool-client';
/**
 * Collection of API clients for additional Audiotool services.
 *
 * @see {@link AudiotoolAPI} for detailed documentation
 */
export type { AudiotoolAPI } from './audiotool-client/api';
/**
 * Utility interface for working with device presets.
 *
 * Provides methods to list and retrieve presets for various device types.
 * See [Presets](../docs-pages/presets.md) for more information.
 *
 * @see {@link PresetUtil} for detailed documentation
 */
export type { PresetUtil } from './audiotool-client/api-utils/presets';
/**
 * Helper consts and functions for using and converting Ticks time
 */
export * from './ticks';
