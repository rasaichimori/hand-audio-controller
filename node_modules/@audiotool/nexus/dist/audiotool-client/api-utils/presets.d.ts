import { DevicePresetEntityType } from '../../document/transaction-builder/prepare-preset';
import { Preset } from '../../gen/audiotool/preset/v1/preset_pb';
import { KeepaliveTransport } from '../../utils/index';

import * as docpreset from "@gen/audiotool/document/v1/preset/v1/preset_pb";
/** A wrapper for the presets API. See [Presets](../../../docs-pages/presets.md)
 * for more information.
 */
export type PresetUtil = {
    /**
     * Lists presets with optional filtering by device type and text search
     * @param deviceType The entity type of the device for which to list presets
     * @param textSearch Optional text to filter presets by name or description
     * @returns Promise resolving to an array of matching presets
     */
    list: <T extends DevicePresetEntityType>(deviceType: T, textSearch?: string) => Promise<NexusPreset<T>[]>;
    /**
     * Retrieves a specific preset by its name or id.
     * @param nameOrId: The identifier of the preset, either an uuid or `presets/{uuid}`
     * @returns Promise resolving to the requested preset
     */
    get: (nameOrId: string) => Promise<NexusPreset>;
};
export declare const createPresetUtil: (transport: KeepaliveTransport) => {
    list: <T extends DevicePresetEntityType>(deviceType: T, textSearch?: string) => Promise<NexusPreset<T>[]>;
    get: (nameOrId: string) => Promise<NexusPreset<DevicePresetEntityType>>;
};
export declare const presetDeviceTypeToDeviceKey: {
    [K in DevicePresetEntityType]: string;
};
export type NexusPreset<T extends DevicePresetEntityType = DevicePresetEntityType> = {
    meta: Preset;
    data: docpreset.Preset;
    entityType: T;
};
